# Screenshot capture

Capture `dashboard.png` only after a real mosaic completes end to end.

- Use a 1440 px or wider desktop viewport.
- Include the heatmap, agent pipeline, verified plan, and action timeline.
- Keep the offline-demo banner out of the primary screenshot.
- Remove personal filenames or location metadata from the visible UI.
- Verify the screenshot is legible at slide size before adding it to the root README.
