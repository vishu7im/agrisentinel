# Training + export deps. Deliberately separate from backend/requirements.txt:
# the backend must stay installable without torch (it only ever needs onnxruntime),
# and torch + CUDA is ~3 GB that a demo laptop should not have to carry.
#
#   .venv/bin/pip install -r ml/requirements.txt
#
# Torch here is the default PyPI build, which ships CUDA. On a machine with no GPU
# the same scripts still run — every one of them falls back to CPU automatically.

torch==2.9.1
torchvision==0.24.1
pillow==12.1.0
numpy==2.3.5

# A2 only: metrics, charts, export.
scikit-learn==1.7.2
matplotlib==3.10.7
onnx==1.20.0
onnxruntime==1.24.1
